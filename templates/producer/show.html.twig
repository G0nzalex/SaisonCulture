{% extends 'base.html.twig' %}

{% block title %}Beer{% endblock %}

{% block body %}
    <h1>Beer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ beer.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ beer.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ beer.description }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ beer.createdAt ? beer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_beer_index') }}">back to list</a>

    <a href="{{ path('app_beer_edit', {'id': beer.id}) }}">edit</a>

    {{ include('beer/_delete_form.html.twig') }}
{% endblock %}
